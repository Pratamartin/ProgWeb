<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>{{title}}</title>
  <style>
    body { margin: 0; font-family: Arial, sans-serif; }
    header, footer { background-color: #0074D9; color: white; padding: 10px; text-align: center; }
    .container { display: flex; min-height: 90vh; }
    nav, aside { width: 20%; background: #DDDDDD; padding: 10px; }
    main { flex: 1; padding: 10px; background: #F5F5F5; }
  </style>
</head>
<body>
  <header>
    <h1>Header (comum)</h1>
  </header>

  <div class="container">
    <nav>Left Menu (comum)</nav>
    <main>
      {{{body}}}
    </main>
    <aside>Right Bar (comum)</aside>
  </div>

  <footer>
    Footer (comum)
  </footer>
</body>
</html>
